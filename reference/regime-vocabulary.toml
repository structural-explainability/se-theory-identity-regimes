# ============================================================
# Regime Vocabulary
# se-theory-identity-regimes
# ============================================================

schema = "se-regime-vocabulary-registry-1"
repo = "se-theory-identity-regimes"
surface_module = "IdentityRegimes.Surface"
namespace = "IdentityRegimes"

[vocabulary.classificationMatrix]
id = "classificationMatrix"
cite_id = "IR.DEF.classificationMatrix"
name = "Classification matrix"
lean_symbol = "classificationMatrix"
source_module = "IdentityRegimes.Transform.Core"
description = "The canonical 9 × 10 classification matrix assigning a ClassificationValue (IGN/PRS/BRK) to every (RegimeProfileKind, Transformation) pair. Single source of truth for classification behavior. Verified counts: PRS=42, BRK=38, IGN=10, total=90. Axiomatic columns: RE=PRS and BF=BRK for all profiles. The REC × PV cell was resolved as PRS: provenance-tracking is the operative mechanism bridging the identity break in recovery, not passive metadata."

[vocabulary.derivedRegimeSet]
id = "derivedRegimeSet"
cite_id = "IR.DEF.derivedRegimeSet"
name = "Derived regime set"
lean_symbol = "derivedRegimeSet"
source_module = "IdentityRegimes.Transform.LowerBound"
description = "The canonical list of all nine derived regime profile kinds: OBL, OCC, REC, ENR_L, ENR_I, CTX_E, CTX_S, NOR_C, NOR_S. Certified complete, duplicate-free, and pairwise non-collapsing under the canonical classification matrix."
